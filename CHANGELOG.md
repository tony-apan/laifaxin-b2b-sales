# Changelog

本公开库版本记录。语义化版本：新功能/工具批次 → minor（v0.x.0）；修复/文档 → patch（v0.2.x）。

## [v0.5.6] - 2026-09-09

新增「一客群一标签一计划」铁律（用户拍板：允许分批、接受成本翻倍）——修"多个客群混存混发"。

- **问题（用户实测）**：客户输入 A/B/C 多个客群后，AI 把三个客群**保存在一起、建同一个计划**，没做区分。不同客群采购需求与话术不同，共用一套等于两个客群的痛点打偏。
- **真实反例**：保温杯 3 个客群（家居厨房分销+户外露营零售+咖啡器具批发）共用 1 个标签 `英语-咖啡器具与保温杯-渠道商` + 1 套话术。
- **真实正例**：皮筏艇 3 客群 → 3 对标签（各自独立）+ 3 套 120 模板（各带独立话术角度）+ 3 条序列，S10 分别加人。
- **新增铁律 7d**（`RULES.md`）：每个被选中的客群**独立成档**——独立一对标签、独立一套 120 模板、独立一条序列，禁止合并。允许用户选多个客群，但**执行必须分批**（一个客群走完 S3→S4→S5→S7→S8→S9→S10 再做下一个）。选中 ≥2 个时**必须提示**"建议分开做"并给"先做 1 个 / 都做（分 K 批）"，如实告知成本 K×120 模板 + K 条序列。选中客群必须落档 `operation-record.md` + `segments/<ID>-<客群名>.md`。
- **S2 卡**：加多客群提示块；开头写明"基于您选的获客方向 <字母>"，并说明本节点是「具体客群」——消除"S0 选过一遍客群，S2 又问一遍"的重复感。
- **S0 卡**：措辞从「客群组合」改为「获客方向」，明确"下一轮会基于方向推演出具体客群清单，不是重复问一遍"。
- **S5/S7/S9 卡**：确认卡标题统一写「<客群名>（第 i/K 批）」；标签名禁止合并多客群；模板前缀/序列名带客群标识（三方呼应）。
- **SOP**：`operations-sop.md` 新增 §3.1「多客群分批执行 SOP」（串行执行顺序 + 皮筏艇分档样例 + 确认节点必答项 + 落档要求 + 例外条款）；`node-playbook.md` 的 S2/S5/S7/S9 各补分批说明。
- **补齐分发改动**：`runs/_template/segments/README.md` 原来是 1 行空壳，补全为完整字段字典（含「多客群归属」三列表 + 混存后果）；新增 `runs/_template/segments/S01-客群档模板.md`；`runs/_template/segments.md` 索引加「模板批次/序列」列。
- **测试 149 → 173**：新增 `tests/test_multi_segment_batching.py`（24 项）锁定铁律 7d 要点、S2 提示块与成本披露、S0 措辞、三张卡的批次归属、SOP 章节、档案模板完整性。变异测试验证断言有效（删铁律/删提示块/删批次行均立刻失败）。两仓各 173 项通过。

## [v0.5.5] - 2026-09-09

按用户实测反馈修三个"小白看不懂 / 流程顺序不通"的问题：**连接平台前置**、**有种子也要有客群标签**、**用户可见文案禁黑话**。

- **连接平台前置为独立节点**：原来"档案确认 → 悄悄连平台 → 突然弹出客群"，用户不知道中间发生了什么。现在档案确认后**立即**按 `output-templates/T-token引导.md`（改造为"连接平台"卡）引导：4 步拿凭据 → 主 AI 走 `check_login` + `workspace_guard --require-verified` + `gate_check` → 展示账号状态卡，**连上才进入选起点**。连接是全流程唯一需要用户动手的一步，放在推演/搜索之前（后面都要联网）。
- **起点引导（用户截图圈出）**：`S0-产品知识档案.md` 确认话术末尾新增"有询盘客户或认得的精准买家官网现在就可以发我"——不追问、不阻断。`S3-种子确认.md` 删掉旧规则"用户没有网址就自动走标准路径、不追问"，改为**入口二选一**：有网址→核对确认并由 AI 判定其所属客群/客户线请用户确认；没有→从已推演客群挑精准买家。
- **有种子也推演客群（方案B）**：`flow_orchestrator.py` 的 S2 不再被 `if not pathA` 跳过——两条路径都推演，有种子时把种子公司名/角色/中英摘要（只读 `domain/base-info`）并进推演输入。原因：S4 匹配率以客群客户线为分子、直采/OEM/拓品分线计数、标签命名、模板痛点加权**都依赖客群标签**，快速路径不能留空。推演失败改为**告警继续**（如实说明、不谎称成功、不推进 S2）；已完成 S3+ 的项目不重复推演。
- **用户可见文案禁黑话（P0）**：用户话术块内清理掉 `status=`/`sha256`/`claims`/`runs/`/`.local/`/`S0a`/`confirmed`/`declined`/工具命令等内部术语（涉及 `S0-画像方案`/`S0a-运营方档案`/`S7-模板确认`/`S11-完成汇报`/`S12-激活确认`），改成白话；内部术语只留在每个模板的"AI 执行要点与边界"。同时明确禁止要求用户设置环境变量（如 `LAIFAXIN_TOKEN`）、禁止声称"只读 token / 只读授权"（平台无此权限级别）。
- **新增 `tests/test_user_facing_copy.py`（17 项）**：自动扫描 `output-templates/*.md` 用户话术块，禁止内部术语、环境变量要求、只读授权声称；校验连接卡要素/检查顺序、档案卡起点引导、S3 主动引导、flow 的"连接早于推演""两条路径都推演""推演失败不推进 S2""已完成项目不重复推演"。变异测试验证扫描器能真抓到违规（塞 `status=` 或环境变量要求即失败）。
- 规则同步：`RULES.md`（S1/S2/S3 判据 + 面向用户输出纪律新增禁黑话条款）、`SKILL.md`（流程图 + 节点表）、`specs/node-playbook.md`、`output-templates/README.md`。
- 测试 132 → **149**；两仓同步后各 149 项通过。

## [v0.5.4] - 2026-09-09

修两个"工具不拦、真实产物出错"的主链缺陷：**工作空间落点从未被校验**、**开发信 CTA 短关键词从未被强制**。

- **工作空间落点校验（P0，用户实测事故）**：AI 拿到企业空间 orgId（如 <企业空间ID>）后，客户/模板/序列却全部建进了 token 所属**个人账号**。根因：`?uid=<orgId>` 只是请求参数、不是信任边界——token 对该 org 无权限时平台**静默落回 token 自己的空间**，且仍返回 `success:true`、参数回显一致。因此"接口成功+参数一致"证明不了落点正确，而当时所有写入工具**根本没做过登录/空间校验**（只有 flow/onboard 调 check_login）。
  - 新增 `tools/workspace_guard.py`：只读 `benefits/refine-data` 的 `data.isOrg`，比对"声明 orgId"与"token 中段用户UID"。`isOrg=false 且 org≠uid`（声明企业却落个人）或 `isOrg=true 且 org==uid`（自相矛盾）→ **阻断写操作**；平台未返回 `isOrg` → 记"未校验"，**不冒充通过**。
  - 九个写路径全部接入 `preflight()`（审批之后、任何写请求之前）：`gen_templates` / `tag_add` / `save_first_n` / `contact_add` / `build_sequence` / `activate_sequence` / `rebuild_templates` / `delete_all_products` / `segments_infer`，以及 `flow_orchestrator` 的 S2 推演写。静态测试逐个核对"必须调用且早于首个写接口"，防漏网。
  - `gate_check.sh` 新增 `[2b]` 强制校验；stdin 只读一次后用变量分发给登录校验与空间校验（`IFS= read -r -d ''` 保留原始字节）。
  - 汇报口径收紧：只能说"落点校验通过"或"未校验"，禁止用"参数回显一致"冒充空间正确。
- **CTA 短关键词闸门（P0，真实产物整批违规）**：`runs/*/template-plan.json` 的真实变体是**零 `<b>`、无短关键词**的开放式要求（"Reply with your target cup sizes…"），而旧工具两处静默放行——`if bolds and (bolds<2 or bolds>4)` 让 **0 处加粗不报错**；关键词提取在 `if m:` 内，**提取不到就整段跳过**（且 `to?` 正则让 `Reply CATALOG` 这类无引号写法永远匹配不上）。实测复现：零加粗文案被判"22 句全部合规"并 exit 0。
  - `bolds` 判断改为 `bolds < 2 or bolds > 4`（0 处必错）；新增 `_cta_keyword()` 识别带引号/不带引号/中文关键词，**缺失、未加粗、超过 1-2 个单词**均 exit 2；`— yes or no is enough` 是非问按规范豁免。
  - `specs/sequence-config.md` 明确"每封必须给恰好一个 1-2 词短关键词、每轮换词、开放式要求不算 CTA"。
- **新增本地模板预览**：`tools/render_html_preview.py --plan <plan.json> [--rounds 1-4 --variants 1,2]` 生成 `preview.html`——加粗卖点/回复关键词黄色高亮、CTA 段蓝线标注，浏览器直接看收件人视角；S7 展示话术已接入。
- 测试从 101 增至 132：新增 `test_workspace_guard.py`（17 项，含误路由阻断、未校验不冒充通过、写工具覆盖与顺序）、`test_cta_keyword_gate.py`（8 项，含零加粗/未加粗关键词/开放式要求回归）、`test_render_html_preview.py`（6 项，含转义与标记泄漏）。两仓同步后各 132 项通过。

## [v0.5.3] - 2026-09-09

用“宠物食品包装袋”在隔离副本做 S0-S12 全流程离线模拟，真实运行本地工具/状态机/审批/模板/manifest，平台写操作使用明确网络桩且不冒充线上成功。模拟发现并修复四类主链断点：

- **昵称闸门绕过**：`Tony-包装厂` 原先能通过公共昵称校验并一路 dry-run 到 S12。现在英文+中文混合昵称一律拒绝（常见“人名+公司/产品”拼接），保留 Tony / Jean-Pierre / 老王 / 张伟等纯昵称；新增回归测试。
- **flow 参数契约误导**：`--product` help 原写“产品名”，实际必须与 `profile.product_key` 逐字一致，传中文展示名会被判跨产品。现明确 `--product=<稳定product_key>`，用户产品说明/展示名走 `--product-info`；SKILL/node-playbook 同步。
- **产品档案 partial draft 假失败**：部分字段填入后先跑独立 validate 会因 frontmatter 来源状态尚未重算而报错。SOP 改为直接由 `confirm` 做写入前结构/来源校验并重算，再 `validate --require-confirmed` 终检。
- **S4/S11 模拟证据洗白 P0**：原 finalizer 只看关键词+hash，证据正文明确写“离线网络桩、不代表线上”仍能推进 S4/S11。现在 audit/verification manifest 顶层强制 `evidence_mode:"live"`，simulation/mock/stub/离线/网络桩/占位/未实际等 mode 或正文标记一律拒绝，record bytes+mtime 不变；模板、RULES、SKILL、话术同步。
- **S11→S12 合法审批出口断路**：原 flow 从 S11 续接仍会重跑 S2/S3 并因状态不符退出，标准工具链无法合法签发 S12。新增 `flow_orchestrator --resume-s12`：仅标准 profile + record严格S11 + seq + live合规文件 + 72h + 当前真实TTY 才签绑定凭证；不联网、不激活、不重跑S0-S10，record保持S11；通用 `approval.py grant` 仍禁止S12。
- **S12 合规模拟洗白与联网顺序 P0**：新增 `compliance_validation.py`，flow/activate 共用 `evidence_mode=live` + 五项72h真实 source/detail 校验；模拟/离线/桩/占位拒签和拒激活。activate 重排为 record/profile/compliance/确认/审批/二次hash 全部本地过闸后才首次联网，失败输入不再先碰平台。
- **模拟结论严格分层**：离线模拟可验证安装菜单、选填输入、网站旁路、档案状态、S0-S12向导、审批参数hash、S7真实模板预览、tmap网格和拒绝闸门；不能证明真实登录、名单质量、保存、线上模板/序列、联系人加入、live合规或激活。simulation只能出报告，禁止正式收口。
- **仍公开的主链缺口**：S3 还没有独立 finalize 工具；S5 防重复保存仍只在规则/人工检查，`save_first_n.py` 未自动查询历史任务。这两项不在本次模拟中伪装为已闭环。

## [v0.5.2] - 2026-09-08

强化凭据交付：用户在浏览器一键复制后，**只需把 `accesstoken=` + `orgId=` 两行整段直接粘贴到当前受信任主 AI 聊天框**，不再设置变量、创建 `.env`、执行工具命令或拆参数。

- 新增 `credential_input.py` 严格解析单一真源：LF/CRLF/CR、重复别名、未知行、null/空值、控制符、非法 UTF-8、8KB 上限、token 三段和 orgId 必填统一 fail closed；彻底删除 token 中段回退。
- `check_login.py --credentials-stdin` 成为默认 AI 入口；无显式 flag 不偷读 stdin，TTY 快速拒绝不挂起。网络改为 urllib 内存 header，token 不再进入 curl 子进程 argv；401/403 JSON 正确分流为凭据失效，网络/空响应保持独立。
- `gate_check.sh` 与 `check_rules.sh` 复用同一登录入口并原样透传 stdin；Shell 不拆凭据。`check_rules.sh` 无凭据时明确只做离线检查，不虚报登录有效。
- 输出不回显原始 token，账号 UID/orgId 只显示掩码；失效计数文件仅存 token 短哈希/次数/时间，采用原子替换和 0600 权限。
- 用户话术统一为“浏览器复制→当前聊天框直贴”；禁止文件、`.env`、持久环境变量、日志和子代理扩散。聊天服务是否保存对话取决于宿主隐私政策，不作虚假零留存承诺。
- 清理三项 P0：旧 data-structure 降为历史并删除 token 落盘设计；审查子代理只收主 AI 提供的脱敏线上证据；只发 token 缺 orgId 时一律停止，不再默认个人空间。
- 活跃 INDEX/SKILL/RULES/SOP/FAQ/工具索引和历史教程警示同步。后续业务工具的 `<TOKEN_IN_MEMORY>/<ORG_IN_MEMORY>` 仅为主 AI 内部兼容占位，不得让用户处理；全工具 stdin 化作为后续独立迁移，不在本版夸大。
- `flow_orchestrator` 的二次登录检查改经 stdin，平台请求改 urllib，避免再把 token 复制到 curl/子检查 argv；其自身交互入口保留内部参数兼容，以维护 S12 当前 TTY 确认边界。

## [v0.5.1] - 2026-09-07

修复 README 极速开始把“安装 Skill”和“开始新获客项目”混在同一代码块的问题：

- 安装代码块只执行下载/更新、依赖准备、规则学习、自检和旧项目扫描；不再出现“我卖的是/目标市场/官网”等业务输入。
- 安装完成后 AI 先汇报安装目录、版本、环境、自检、能力、确认边界和旧项目，再只问“安装完成。你现在想做什么？”，展示新项目/适配判断/网站提炼/续接/更新换机/了解流程六项菜单。
- 安装或学习阶段禁止索取产品、市场、官网、昵称、token，也不得默认进入 S0；用户选择具体任务后才按 SKILL 路由渐进询问。
- `onboard_check.py` 同步改为任务菜单：有旧项目只报告、不自动续跑和索取 token；无旧项目不自动建项目。README 流程图明确仅描述用户选择“开始新项目”后的获客流程。
- RULES、SKILL、environment-setup 同步区分安装入口与业务入口。

## [v0.5.0] - 2026-09-07

新增**独立可选的网站资料增强模块**，不改变 S0-S12；没网址、用户跳过、读取失败或模块异常时，原流程照常运行：

- **输入收口**：必填只有纯个人昵称 + 卖什么；卖给谁、卖到哪、用户自己的官网/产品页/目录全部选填。缺客群或市场时 AI 先给有理由的建议，不阻断、不反复追问、不冒充用户输入。
- **网址角色隔离**：固定 `OWN_COMPANY_SITE / OWN_PRODUCT_PAGE / OWN_CATALOG / BUYER_SEED / THIRD_PARTY_REFERENCE / UNKNOWN`；用户声明是角色真源，普通网址默认 UNKNOWN，买家网址只转 S3，第三方参考只做分析。
- **无网络模块**：`website_profile.py` 不爬站，只接收 AI 已读取公开页面后生成的 JSON；六区隔离 facts/company_claims/ai_analysis/recommendations/conflicts/unverified，硬事实必须带具体 URL 和页面原文。
- **批准补丁链**：候选校验 → 公司/产品分开 prepare → 展示 patch id/hash → 明确批准 + ack → canonical hash/base hash/project/标准路径重验 → 正式档案单文件原子写入。冲突、未核实、第三方联系、哈希漂移、跨项目、错误副本全部 fail closed。
- **主链零干扰**：产品补丁导入后回 draft，仍须现有 confirm；模块只生成影响建议，不修改 operation-record。S12/ACTIVE 拒绝产品更新，必须先停用。公司档案跨产品共享，已有任何项目记录后拒绝自动导入。
- **现有档案硬化**：confirmed 必须记录 confirm_quote；declined→confirmed 升版；S12/ACTIVE 的 confirm/decline 均在写前拒绝，防止本地改档但线上旧序列继续发送。
- README/RULES/SKILL/SOP/话术/机器索引/迁移说明同步；新增 JSON Schema、运行模板和本地回归测试。

## [v0.4.16] - 2026-09-07

README 按“小白 3 秒看懂、30 秒能开始、细节按需展开”做结构级重写，并同步修复重写时发现的执行层冲突：

- **唯一开始路径**：原“下载代码 / 准备 4 样东西 / 交给 AI”三套重复步骤，合并为一段可直接发给 AI 的简短指令；Git/ZIP/Python 等技术细节只在下载失败时展开。
- **信息分层**：主文只保留价值、开始、完整流程、产品适配、合规边界和常见问题；名单阈值、模板数量、文件路径、费用参考、更新与换机长指令均按意图折叠。
- **四个写操作确认点对齐**：流程图逐一展示保存名单、创建计划、加入联系人、激活发信；明确激活前不发送，激活后才按已确认计划自动跟进。
- **费用与验证口径降级为参考**：15 天体验、399 元/年、约 2.3 万赠点、1 万联系人、48 小时/3 个询盘均注明截至日期、适用前提、理论上限和平台实时确认要求，不作为永久权益或行业基准。
- **token 时序统一**：对话开局先收昵称和一句话产品、完成产品了解与适配；首次调用来发信平台前再获取 token 并验证。RULES/SKILL/onboard/flow/决策树同步，不再开局催账号钥匙。
- **企业空间 P0 修复**：`gate_check.sh` 禁止再把 token 中段用户 UID 当 orgId；只接受“一键双取两行整段”或显式 `--org`。动态验证纯 token 会阻断、企业 orgId 正确入 API、显式 org 正确覆盖。
- **老用户红线保留**：更新仍要求原目录覆盖、先备份、冲突停手；换机仍按白名单恢复、token 重取、旧审批降级、记录不一致进入 ERROR_BLOCKED。

## [v0.4.0] - 2026-09-04
换机续接与资料回落成为**可执行主链**，不再只是文档承诺：
- **零 Python 环境准备**：新增 `tools/bootstrap.sh`（macOS/Linux/WSL/Git Bash）与 `tools/bootstrap.ps1`（Windows PowerShell），先 check-only、缺项自动安装、安装后复查；新增 AI 专用 `specs/environment-setup.md`。macOS `--check-only` 已实测全绿；PowerShell 脚本在本机无 Windows/pwsh，当前仅完成静态语法结构审查，未声明 Windows 实机通过。
- **换机续接**：新增 `specs/migration-handoff.md` 与 README 旧机/新机两段完整复制块；迁移 `.local/`、`runs/<operator_key>/`、可选本地 `db/`，token 新机重取；`onboard_check.py` 自动枚举 operation-record 状态与 profile 版本，禁止从 S0 重跑已有项目。历史 approvals 只作审计，未执行写节点和 S12 必须新机当前对话重新确认。
- **公司级资料档案**：新增 `tools/operator_profile.py` + `specs/operator-profile-sop.md` + `S0a-运营方档案`话术；AI 分轮主动索取公司名/官网/用户自己的联系邮箱/默认市场语言，回落 `.local/operators/<operator_key>.md`（多公司隔离，旧单文件兼容）跨产品/换机复用，不保存 token 或第三方资料。
- **产品级资料状态机**：新增 `tools/product_profile.py` + `profile_utils.py`，product-profile 必须 `draft→confirmed` 或 `declined` 才能进入 S1；记录 operator/product稳定键、版本、字段级 source/confidence、确认原话、正文hash和append-only变更记录；补资料后二次确认自动升版本，operation-record 自动推进状态。
- **模板/序列硬闸门**：`flow_orchestrator.py` 必传 profile；`gen_templates.py` 移除含无来源 ISO/交期/折扣/稀缺等事实的内置 PRODUCTS，生产只接受绑定 profile hash 的 plan；高风险事实 claims 必须逐句绑定档案字段/source/evidence_text；签名昵称硬校验，渲染末段只有纯昵称；`build_sequence.py` 校验 tmap.meta/profile hash/status/稳定项目键，档案更新后旧模板映射不可复用；修复规则回读成功反而退出的旧控制流。
- **签名/正文精确边界**：邮件末尾签名区永远只有纯个人昵称；公司名/官网/联系邮箱不进入签名；经用户确认且有字段级来源的认证/产能/MOQ/交期/价格带可用于正文卖点；推断或无来源的具体事实禁止写入。
- README/SKILL/RULES/INDEX/node-playbook/sequence-config/operations-sop/gate/check_rules/运行模板/机器索引同步装配。主链完成 Python/Shell 编译、macOS bootstrap、19 项离线失败路径矩阵、档案二次升版与模板签名/claims 实测；独立审查发现项继续见本版本发布说明。

## [v0.3.12] - 2026-09-04
**【更正】邮件签名=只有昵称（v0.3.11 口径错误回滚）**：v0.3.11 误把"公司名/官网/邮箱等可写进正文/落款由用户拍板"。准确边界是：**邮件末尾签名区只有纯个人昵称，禁止公司名/官网/邮箱/职位/认证、宣传语等任何其他内容**；公司名/官网/联系邮箱供 AI 建立运营方档案、读取网站和分析，不进入签名；认证/产能/MOQ/交期/价格带等产品事实写入 product-profile，只有经用户确认且有字段级来源时才可用于正文卖点，无来源或推断的具体事实不得写入。
- 覆盖纠正：RULES（S0/S7/运营方档案/产品知识档案 4 处）、SKILL（mermaid/S0/S7/铁律7a/产品资料段/运营方档案 7 处）、specs/product-profile-sop.md（2 处）、output-templates（S0-产品知识档案/S0-画像方案/S7-模板确认/README 全局规则）、tools/onboard_check.py（2 处）。
- 校验：grep 全仓确认不再有"可写进正文/落款/展示带不带由用户拍板"残留；"可主动要"均标注"仅供 AI 建档/背调、绝不进邮件签名"。

## [v0.3.11] - 2026-09-04
「主动要」边界修正（回应：为什么不能要公司名/官网/邮箱等）+ 依赖自检强化：
- **边界修正**：原"禁止主动索要公司名/官网/邮箱/认证/产能/MOQ"是认知错误——这些全是**用户自己的商业资产**（商业名片+开发信弹药：认证证书、产能、MOQ、交期、价格带恰是开发信最有杀伤力的点）。改为：**可主动要**（用户自己的资产，给填空模板+示例、可跳过、不逼问），但**写进正文/落款须在 S7 展示带/不带两种效果样例，由用户拍板**（尊重 v0.2.13 落款只用昵称）；**永不索要（他人信息）**：潜在买家/客户/联系人邮箱。改动覆盖 RULES（S0/S7/运营方档案/产品知识档案）+ SKILL（mermaid/S0/S7/运营方档案）+ specs/product-profile-sop.md + output-templates（全局规则/S0-画像方案/S0-产品知识档案/S7-模板确认）+ tools/onboard_check.py。
- **产品知识档案 SOP**：新增 `specs/product-profile-sop.md` —— 专给 AI 的提炼方法/主动要模板/8 字段校验/回落/与对抗审查对接完整 SOP。
- **依赖自检强化**：`tools/onboard_check.py` 自动探测 python3/Windows 常见 `py`，缺 python 时按平台给出安装指引（Windows 加 PATH / macOS xcode-select / Linux apt,yum）；README 运行节补 Windows `py` 启动说明 + 自检入口。解决"python 命令没找到"（Windows 常见）。

## [v0.3.10] - 2026-09-04
**产品知识档案（product-profile）回落构建机制**（用户给目录/网站 → AI 提炼 → 回落可复用）：
- RULES「多公司/多产品」新增产品知识档案机制：用户主动给官网/产品目录/卖点 → AI 读取提炼成 `runs/<运营方>/<产品>/product-profile.md`，8 字段规范（定位/产品线/卖点/客群市场/规格认证/差异化/合规禁忌/可引用数字）+ 来源标注（用户·官网·推断⚠️）——数字与认证无来源不写。
- SKILL：S0 行升级为"读取分析并落成 product-profile → 请用户确认 → 反哺 S2/S4/S7/S9"；S2/S7 显式注明从该档案取知识（客群更准/卖点更真/钩子贴合）；断点续接先读它。
- 新增话术模板 `output-templates/S0-产品知识档案.md`（AI 提炼 8 字段→用户重点核对③卖点⑧数字→回落）；output-templates 总索引挂上。
- `runs/_template/product-profile.md` 占位 + INDEX 目录导览 + SKILL 档案行补 product-profile。

## [v0.3.9] - 2026-09-03
README「给技术朋友（附录）」重构为四组结构化（🔧运行 / 📁目录导览 / ⚖️发布与合规 / 🔒安全边界）——原先 8 段粗体+长段落挤在一起、无分组无层级，改为表格+短列表，读者可按主题定位。

## [v0.3.8] - 2026-09-03
SKILL.md frontmatter 兼容 codex 加载器：补 `name: laifaxin-b2b-sales`（ASCII 稳定标识）+ `version: 0.3.7`。此前 SKILL.md 缺 `name` 字段，codex 的 skill-installer 检查时报"缺少 name 字段"，会尝试补兼容字段。保留 `title`（人类/其他工具用）不变，frontmatter YAML 解析已校验。

## [v0.3.7] - 2026-09-03
README「🔄 更新到新版本」升级为**完整可复制指令块**（与"第一步安装"办法 A 对称）：6 步写死——判断安装方式（.git→git pull / 无→ZIP 覆盖到原目录，禁解压新文件夹）→ 定位系统文件夹（不猜路径）→ **先备份 `.local/` 与 `runs/` 到系统文件夹外**（成功后可删）→ 按方式更新（git 冲突先停不强推）→ **ZIP 覆盖后查"新版已删除的旧文件"残留、列出问用户再清** → 体检 + 汇报版本/变化/数据完好。办法 B 保留一句话触发。SKILL.md 路由表更新行同步补齐备份与残留检查细节。

## [v0.3.6] - 2026-09-03
老用户更新能力补全：SKILL.md 路由表新增"更新到最新版/升级/老用户更新"触发行——AI 判断安装方式（git pull vs ZIP 覆盖解压到**原目录**）、保留 `.local/` 与 `runs/<你的名字>/`、体检汇报版本。此前该指引只写在 README 括号里，零上下文 AI 的第一入口路由表查不到。

## [v0.3.5] - 2026-09-03
产品适配度判定（对抗审查驱动，用户关切"大宗/长周期工业品适不适合"）：
- 新增 `specs/product-fit.md`：S0 前置三档判定（强/条件/弱适配）——四问（买家盘子/谁回冷邮件/采购周期/渠道层）。明示"消费 vs 工业二分不成立"（步进电机=工业标准组件实测跑通约 4000 家/8498 邮箱）；弱适配默认小样验证（≤500 家/1 轮）。
- RULES.md S0 加适配判定线、S9 加周期边界、子规则表挂 spec；SKILL.md S0 行/路由表加适配触发词、S9 周期注、文件地图挂 spec。
- 话术模板：S0-画像方案加适配提示（三档结论+弱适配小样选项）；output-templates 总索引补挂 S4/S8 过程话术；S2 周期列如实填+周期长含义注；S9 序列节奏边界提示。
- 边界表述：README「适合你，如果你」后加"更适合/不太适合"画像+判断口诀、FAQ"支持卖什么"补市场形状边界；docs/09 §8 加"隐含前提"边界节。
- 成本口径补全：docs/09 成本表加脚注"仅发送通道，完整投入=发送+保存点数+人工"；FAQ 3000 封条目同步。
- 昵称口径残留清理：wiki/faq、docs/06 仍教 `Tina from ABC Corp` → 统一为"只放纯人名，公司信息放正文"。
- 其他对抗残留：S3 模板表格竖线数错误（整表不渲染）；SKILL P2-4 括注笔误；docs/00 序列节奏过时口径；docs/08 保存"界面 Playwright"过时行；FAQ 单域名上限 2~10 → 默认 5。

## [v0.3.4] - 2026-09-03
四组整体对抗审查（README/SKILL/模板体系/两库卫生）整改：13×P1 清零——node-playbook S0 改渐进口径；新增 S9a 固定标签节点；S12 凭证闭环（activate_sequence 原话通过自动落 S12 审批行）；CHANGELOG 回填 v0.2.9~v0.3.3；SKILL 语言三方配对 7c 补全；check_rules.sh 虚标"一条龙"去除；S3 话术幽灵档案引用修正；模板索引漏挂补齐；教训条数/白名单/tag 树同步。

## [v0.3.3] - 2026-09-04
开局指令块对抗整改：指令块补"已发本地路径则跳过下载"（防 ZIP 用户被另克隆）；快速入口"3样/4样"统一为4样；指令块补 token 时机；max3 加升阶括注；模板计数口径统一（15 话术模板+1 总索引）。

## [v0.3.2] - 2026-09-03
第一步安装改用户视角：办法 A=把页面链接发给 AI 说"帮我把系统下载到电脑"（git clone 成为 AI 的事）；办法 B=Code → Download ZIP 两下鼠标。用户从安装起零命令行。

## [v0.3.1] - 2026-09-03
「更新到新版本」章节改用户视角：用户只说「帮我把系统更新到最新版，保留我的数据」，AI 自动判断安装方式/拉新版/跳过本地数据/体检汇报。

## [v0.3.0] - 2026-09-03
- README 新增「🔄 更新到新版本」与入口表更新行
- onboard_check.py 开机输出"核心规则热身"四条 + 对用户固定开场白（把用户当小白）

## [v0.2.18] - 2026-09-03
README 二维码缩小至 160px。

## [v0.2.17] - 2026-09-03
SKILL.md 全面强化梳理：渐进图补 S4/S8/S11 终点；两个必出示产出（账号状态卡/数量账）；路由表补账号状态/数量账/归组；状态机 S6 补数量账义务；铁律 7a 署名/7b 邮箱口径；工具与文件图更新；终审 13 项清零。

## [v0.2.16] - 2026-09-03
README 内链补全：详细逻辑表与技术附录目录导览全部改为可点击链接；新增 output-templates/ 导航行。

## [v0.2.15] - 2026-09-03
README 顶部升级：shields.io 徽章行；Mermaid 加"你提供"节点与询盘网址回流扩量支线；快速入口按用户旅程排序。

## [v0.2.14] - 2026-09-03
模板自动归组：gen_templates 默认按前缀建/复用同名分组（禁散落"未指定目录"）；规范与话术同步。

## [v0.2.13] - 2026-09-03
四条实战拍板：①公司触发器=什么都不做（含回读校验）②官网/卖点 AI 即用分析（冲突以用户口径为准）③激活后当天看数据+首日样本参考 ④昵称=纯个人称呼。

## [v0.2.12] - 2026-09-03
输出模板体系终审收口：14 模板全带执行要点；渐进图补 S2/S11；S5 补标签行；S7 去硬编码昵称。

## [v0.2.11] - 2026-09-03
保存数量账透明化：四机制（max3/验真/去重/异步）主动解释；未知邮箱默认已存；S6-数量账模板；save_first_n 内置输出；<1.0 邮箱/家查锚点。

## [v0.2.10] - 2026-09-03
账号状态展示固化：check_login 显示 SVIP(vip2)/配额剩余/充值状态；S0-连接成功话术模板（展示边界：无余额/到期字段不编造）。

## [v0.2.9] - 2026-09-03
渐进索取：开跑只问 token+昵称+一句话产品；每节点一事一问；禁索公司名/官网/邮箱；落款只用昵称；激活前 AI 逐项自查；修复编造 MOQ 洗白链。

## [v0.2.8] - 2026-09-03
README 首屏重构（用户反馈"乱糟糟"）：

### 变更
- 新首屏：一句定位 + 这是什么/不是什么 + 适合谁（3条）+ 6节点 Mermaid 流程图 + 3 个入口。
- 移除首屏宽对比表、完整成本表、长段落解释——下沉到 docs/09 专题文档。
- 移除 README 完整版本历史表：版本细节统一放 GitHub Releases 与 CHANGELOG，README 只留 Latest Release 入口。
- GitHub 实测：Mermaid 正常渲染、无横向溢出、暗色主题可读。

## [v0.2.7] - 2026-09-03
广撒网与精准跟进双引擎（用户拍板）+ 三轮对抗审查整改：

### 新增
- 专题 `docs/09-mass-outreach-to-precision-follow-up.md`：成本账、询盘不等于订单、公司/联系人背调、A/B/C/D 分级、邮件/WhatsApp/商务社媒/电话长期跟进。
- 询盘小白话术模板 `output-templates/Q1-Q5-询盘精准跟进.md`。
- Mermaid 主逻辑图升级为“双引擎”：批量获客找信号 → 标签停邮件 → 精准背调与长期转化；新增成本分工图。
- SKILL/FAQ/INDEX 路由，让后续 AI 能回答询盘失联、背调和多渠道跟进问题。

### 真实能力与合规修正
- 系统不会自动识别回复或自动打标签：人或 AI 助手发现回复后必须立即打“询盘”标签，标签实际生效后 notSentTags 才停后续邮件。
- “不发”标签只自动阻止邮件；WhatsApp/社媒/电话的拒绝记录与停止由业务员人工维护。
- WhatsApp 仅在已有明确许可后使用；电话按目标市场核验拒呼登记、身份披露、号码显示与联系信息；邮件同样核验目标市场、主体类型、名单来源和实际退出方式。
- 平台负责发送技术与退订呈现，运营方仍承担名单、内容、目标市场、拒收和数据处理合规责任。
- 成本数字仅为用户运营估算；询盘没有收入，不能证明回本，最终看成交毛利与完整运营成本。

### 流程收敛
- Mermaid 失败分支改为明确失败关闭；Q1 先打标签停邮件、Q2 再登记；S3 三步搜索链与 S9a 账号固定标签补进主图。
- 修复 S9/docs06/glossary 等旧“回信自动停发”“不怕封号”残留；自由发挥不能跳过写操作确认。
- 建立知识晋升规则：通用问题/FAQ/SOP 脱敏进入公开版；真实客户、询盘、审批和账号状态仅留私有库。

## [v0.2.6] - 2026-09-03
产品定位澄清（用户拍板）：

### 声明
- **README 顶部新增定位声明**：本系统=**批量获客（广撒网）**，不是精准开发——按客群批量找买家、模板化群发、12 轮计划发送，目标是低成本铺量筛出询盘信号；**人或 AI 助手发现回复后须立即打“询盘”标签，标签实际生效后才停止后续邮件，精准跟进靠业务员人工**。
- 新增「发了就能成单吗」FAQ；系统不承诺回复率，SKILL 写入统一定位话术。

## [v0.2.5] - 2026-09-03
四组空白代理全程终审整改（清空/激活/SOP/发版/工具代码逐环节对抗）：

### 修复（P0）
- **激活确认原话否定句绕过**：activate_sequence 移植否定/暂停/犹豫词校验（"先不要激活"不再放行）——实测拒绝。
- **新增 `--deactivate` 回滚**：空序列测完回滚 inactive 工具化（回读防假+幂等免凭证），不再手敲接口。

### 修复（P1）
- wait_save_done 形状异常不再裸崩（对齐 contact_add 硬化）。
- S3 旧表述七处清理（"必跑反查"/编排器旧提示/决策树判据/operations-sop 残留）——全库统一四步路径。
- RULES S3 状态机固化为统一 AI 数据库搜索四步（含相似列表禁令）——唯一真源到位。

### 改进（P2）
- v0.2.1 Release/CHANGELOG/README 标注"已被 v0.2.2 取代"；CHANGELOG 语义句修正；SKILL 写入断言分级纪律；账号终态盘点入档。

## [v0.2.4] - 2026-09-03
C1 补测闭环——S3 SOP 全链路 100% 实测：

### 实测（当日）
- **长文本关键词保存生效**：客群画像长句作 keyword 保存前 10 → 10 家/24 联系人落库；保存对象与列表第一页**同批**（域名逐家核对一致）。
- 附带发现：保存后的公司视图自带域名字段；其标签过滤视图仅显示有域名记录的公司（展示口径，非数据丢失）。

### 修正
- S3 第④步"待实测"标注移除 → 双 keyword（域名/长文本）均实证。
- 测试数据已清理（24 联系人+2 标签），账号回到流程保留态。

## [v0.2.3] - 2026-09-03
S3 SOP 定稿 + 臆想断言对抗自查（用户逐条拷问驱动）：

### 补测（当日成立）
- 客群英文画像句（query_en）**长句直接当关键词**：✅ 实测 9996 家、首页 10 条全为高匹配买家。
- 公司详情接口（base-info）**双入参**：✅ 传 id 返回域名；**传域名也返回公司信息**（修正 v0.2.2"只能传 id"的片面结论）。
- **域名作关键词找相似**：✅ 实测（theboatpeople.org → SOTAR/Saturn Rafts 等全为筏艇买家）——"找相似"=主搜的域名用法，非已禁的相似列表接口。

### SOP 定稿（四步）
① query_en 关键词 → 搜索第一页（25 字段/条）② 代表买家 id → 详情接口取域名 ③ 域名作关键词再搜主搜（相似扩量）→ 审计 70% 临界 ④ 保存（关键词=域名已双产品实证；**长文本关键词保存⚠️待实测**——首跑小 N+对账）。

### 诚实标注
- L-50：臆想断言清单入档；文档断言分级（实测✅/引用📚/推断⚠️/假设❓）；列表接口验证≠保存接口验证。

## [v0.2.2] - 2026-09-03
S3 链路定稿（用户实测拍板：统一 AI 数据库搜索，禁用相似列表）：

### 修正
- **取域名正道钉死**：公司详情接口（domain/base-info）的入参是**搜索结果项的 id**（字段名叫 domain 但值传 32hex id）——返回真实域名+公司名/行业/中英摘要。旧文档写成传域名是错的。
- **S3 统一路径**：客群 query_en 关键词 → AI 数据库搜索第一页（每条含 id）→ 需要域名时逐条 id → 详情接口即得；保存按关键词前 N。移除对相似列表接口（domain/similar-list）的依赖。
- `seed_resolve.py` 重写：`--id`（id→域名+详情）/ `--keyword`（搜第一页+逐条域名，一条命令全览）/ `--company`（兜底）。
- 文档五处同步（SKILL S3 / 操作手册 / 保存前清单 / 对抗审查清单 / api-reference 接口标注）。

### 教训
- L-49：接口字段名≠值的类型，以实测 payload 为准；UI 列表显示的域名背后是"列表+逐条详情"组合调用。

## [v0.2.1] - 2026-09-03
> ⚠️ **本版"相似列表自带域名"方案已被 v0.2.2 取代**（similar-list 已不使用）——仅作历史记录，请用 v0.2.2+。
S3 选种子流程纠偏（用户实测反馈）：

### 修正
- **双路径取代"必经域名反查"**：① 小批量——客群推演的英文画像句（query_en）即搜索词，首页即高纯度买家，直接按文本关键词保存前 N，**全程无需域名**；② 扩量才需域名锚——**相似客户列表（domain/similar-list）每条结果自带域名**，挑中直接用，无需名字反查。
- `seed_resolve.py` 降级为兜底工具（仅手头只有公司名时用），不再是必经步骤。
- 文档同步：SKILL S3 / node-playbook S3 / 保存前 checklist / 对抗审查清单② / 闸门规则改为"搜索锚双路径"表述。
- 接口能力标注：主搜索接口返回项无 domain 字段（取域名走相似列表或精确查找）——api-reference 明示。

### 教训
- L-48：先穷尽接口原生能力再设计工具链；兜底工具不应固化为强制路径。

## [v0.2.0] - 2026-09-02
正式修复回落（对应对抗审查 ISS-48~53 与教训 L-44~47，脱敏抽象）：

### 修复
- **S2 推理档案建档**：客群推演须用 `inference-product-add` 建"推理档案"（旧用基础档案导致 `inference-segment-generate` 返回 500）；generate 后轮询列表（6×10s）直到非空。
- **审批闸门表头**：审批流水首次创建文件补写表头，并容错无表头旧文件——修复新账号首次使用时审批闸门全部失效。
- **按标签查人 filters 数组**：联系人按标签过滤须用 `filters` 数组 `[{property:tags,operator:include,...}]`（旧 `filter.tags` 返全库 total，导致对账假通过）。
- **清空工具防呆**：delete 类工具统一 `--execute` + `--confirm DELETE-ALL` 双重确认（含 delete_all_products 极性修复）。

### 新增
- `tools/activate_sequence.py`：S12 激活（激活后回读 status:active 防接口假成功；`--status` 只读查；须审批 + 用户明确"确认激活"）。激活接口早期 500 已实测恢复。
- `tools/seed_resolve.py`：S3 种子域名反查——候选公司名 → 精确查找反查真实域名（相似搜索候选无 domain 字段，拿公司名搜会命中同名异司）。

### 改进
- 模板钩子规范：正文首句须为买家视角钩子（痛点/利益），禁止 "We manufacture…" 能力陈述开场。
- 教训 L-44~L-47 入 lessons-learned.md（档案体系/判据固化/种子选型/激活防假等）。

## [v0.1.0] - 2026-09-01
首版发布：净化公开库（通用层 + 演示案例），GPL-3.0。
