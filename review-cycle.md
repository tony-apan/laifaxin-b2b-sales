---
title: "旁观者审查周期（其他AI从旁观者角度审查完善）"
description: "机制：完成阶段/产品后，由另一AI(或其他agent)从旁观者角度审查记录，审查结果回落issues/lessons。用户强制。"
created: 2026-08-29
author: "AI + 用户对抗"
tags: [旁观者审查, 红队, 对抗完善, 审查周期]
status: active
---
# 🔍 旁观者审查周期（Review Cycle）

> **★ 目标**：让"当局者迷"变"旁观者清"——每次关键阶段完成后，由**没有参与执行的其他 AI**从旁观者角度审查，发现问题并回落。

## 触发时机（每个都触发）
- ✅ 每个产品完整流程跑完（金属粉末/皮筏艇/电动自行车）
- ✅ 每次新发现/新机制建立后（闸门/时序守卫等）
- ✅ 用户新指令落地后（如"MOQ结合实际"）
- ✅ 定期（每周）

## 旁观者是谁
- **方案A**：`subagent` 启动一个新的 AI agent（不共享本会话上下文，只看知识库文件）——**只看记录，从旁观者角度挑刺**
- **方案B**：Terra 式红队 prompt（指定角色：审查记录/挑矛盾/找遗漏）

## 审查输入（给旁观者）
1. 本地会话记录（用户指令+问题+待办；不入 Git）
2. 本地问题登记 `db/issues.tsv`（本地数据，不入 Git）
3. 本地运行记录 `db/runs.tsv`（本地数据，不入 Git）
4. `lessons/lessons-learned.md`（教训L-01~L-54）
5. `RULES.md`（强制流程+铁律）
6. 本次执行日志/记录（reflection-*.md）

## 审查要求（旁观者角色）
- **从旁观者角度**：不解释自圆，**只找问题/矛盾/遗漏/掩盖**
- 检查点：
  - 用户指令是否都落地了？有无背离？
  - 问题是否如实登记（含"我犯的错"）？有无掩盖？
  - 流程与记录是否一致？有无跳步？
  - 有没有"看似做了实则没生效"（如国家排除写了但未验证）？
  - 待办是否合理、可执行？
- **输出**：`P0/P1/P2` 问题清单 + 每条依据 + 改进；直接给"必须修"项

## 审查结果回落（必须）
- 新增问题 → 本地问题登记（本地数据，不入 Git）
- 教训 → `lessons/lessons-learned.md`（L-新条目）
- 修正 → RULES.md / specs / tools
- 审查记录 → 本地审查记录 `review-*.md`（留档，后续可查；不入 Git）
- 全部 git 提交

## 🌐 通用知识晋升到公开版

每次发现来发信相关问题并完成实测/对抗后：

1. **应进入公开版**：通用接口纠错、流程 SOP、用户 FAQ、风险边界、输出模板、可复用教训。
2. **必须先脱敏**：租户 ID、token、真实客户/邮箱、标签/序列/模板/任务/审批 ID、运营方姓名和用户原话全部删除或占位。
3. **只留本地/私有库**：真实运行档案、客户名单、询盘原文、审批流水、逐客户背调、账号终态和内部审查证据。
4. **发布门**：净化扫描无敏感命中；内容与逻辑审查 P0/P1=0；README/CHANGELOG/版本 tag/Release 说明一致。

## 执行方式
每次触发：`bash` 启动 subagent（读上述输入文件，输出审查报告）→ 我回落 → gate_check 再确认。
**旁观者=独立 agent**，保证"当局者清"。

## 🤖 空白子代理审查协议（2026-08-31 升级，用户强制：融入每一步）
1. **构造空白代理**：新子代理，无会话上下文。输入包=①仓库路径 ②规则文件清单（RULES 对应节+node-playbook 对应节点+相关 spec）③待审产出全文 ④对应固定审查清单（见 RULES「操作对抗审查」四类清单）⑤只读 token（线上抽验用）
2. **审查执行**：照清单逐项核；文件声明与线上抽验冲突时以线上为准并标 P0
3. **输出**：本地审查记录 `rev-YYYYMMDD-HHMM-<操作>.md`（结论：放行/整改 P0-P2，每项带证据；不入 Git）
4. **阻断与重审**：P0 整改后必须重审；重操作(保存/序列/激活)预审+用户确认双过才执行
5. **防形式化**：审查代理不得给"看起来没问题"类空泛结论——每项清单须引用产出原文+证据
