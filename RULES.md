---
title: "来发信获客·规则总纲（唯一真源）"
description: "所有规则的唯一入口：无论会话长短/换电脑，先读本文件再执行。强制流程+校验清单。"
created: 2026-08-29
updated: 2026-09-03
author: "AI + 用户对抗"
source: "皮筏艇/电动自行车全程"
related: [specs/threshold-method, specs/domain-scale-sop, specs/sequence-config, specs/marketing-rules-2.0]
tags: [规则总纲, 唯一真源, 强制流程, 迁移]
status: verified
audience: 人+AI
---

# 🧭 来发信获客·规则总纲（唯一真源）

> **★ 执行原则**：**每次操作前先读本文件 + 引用的 specs**，严格按流程，**禁止凭记忆跳步**。
> **本文件是唯一真源**，所有子规则从这里引用。换电脑/新会话：先读本仓库对应文件。
> **★ 入口总线**：`onboard_check.py`（环境+文档引导）→ **`check_login.py`（流程第一步=登录检查，无 token 引导用户按[官方教程](https://www.laifa.xin/share/ai/laifaxin-ai-account-connection)获取）** → `gate_check.sh`（闸门，未通过**禁止任何保存/模板/序列/contact-add**）→ `flow_orchestrator.py`（向导）。闸门=开始流程的唯一通行证。
> **★ token 中途失效 SOP**：任何节点发现 token 失效 → 立即停止一切写操作 → 引导用户按官方教程重取 → `check_login.py` 复验通过 → 从**当前节点**继续（已写 approvals 不作废，勿从 S0 重跑重复建产品档案）。

## 🚨 强制流程与状态机（严格执行，禁止跳步）
```
S0 INPUT_GATE: ★第一步=登录检查（tools/check_login.py 只读验证；无token/失效→引导用户按官方教程获取后交给AI）。★最小必要输入=昵称+一句话产品（中英皆可）；缺一只问这一项，不写入。★昵称规范（2026-09-03 用户拍板）：**只含个人称呼**（如 Tony / Iris）；**禁止**公司名/产品名/职位/号码等（如 "Iris | XX Textiles"、"Tony-保温杯厂" 一律要求改）；正文若需公司信息，走"用户主动提供官网/公司名→AI 分析"通道，不塞进昵称。★产品适配度判定（只读前置，出 ABCD 方案前）：按 specs/product-fit.md 四问判 强/条件/弱适配，结论+理由随方案展示；弱适配必须如实告知预期（冷邮件回询盘以月/年计，建议小样 ≤500 家/1 轮验证），用户决定是否继续——不拒绝、不静默、不加输入项。★S0 出 A/B/C/D 获客方向方案（含推荐与淘汰理由）供选字母。★渐进索取（2026-09-03 用户拍板）：每次只问当前节点必需的一件事；禁止开局列清单；禁止主动索要公司名/官网/联系邮箱/认证/产能/MOQ——用户主动提供才选用，没有则落款只用昵称、正文不写这些字段、不编造具体数字。★例外：S0 方案末尾可提供**一次**官网/产品页入口（可选贡献，不追问不催促），用户给了即触发 AI 读取分析
S1 PATH_PENDING: 有精准网址→快速路径A；无→标准路径B自动选择不追问（用户随时可补网址切换）；两条路径的产出都会在后续节点展示确认
S2 SEGMENT_PENDING: 标准路径推演默认4个；打印全部，判断是否精准潜在客户，给成交周期/询盘速度/量级/邮箱/竞争度/推荐；用户确认或要求更多
S3 SEED_PENDING: ★种子发现·统一 AI 数据库搜索链（3步）（2026-09-03 用户拍板定稿，取代旧"文本搜+域名搜"两段式与相似列表接口路径）：①**预览**——客群 query_en 长句直接作关键词搜索第一页 10 条（每条含权威id/公司名/国家/角色/NAICS/客户类型/置信度/邮箱数/电话/社媒/摘要/匹配分；列表项无域名字段；长句直搜已实测）——按**客户线**评估候选（直采/OEM/拓品三线皆可为锚；S3 不加排除4区 filter，S4/S5 才排除）；②**取域名**——代表买家的 `id` 调公司详情接口（传 id 或域名均可）即得真实域名+详情；③**找相似**——域名作关键词继续走主搜索（★不使用相似列表接口）→ 用户确认锚点。随后进入 S4 审计；S5/S6 再按审计所用关键词保存前 N（域名/长文本均已实测：保存=列表同批）。勿拿公司名当搜索锚（同名多司）；禁翻页收集 id（铁律3）不受影响。
S4 AUDIT_RUNNING: 只读搜客和AI语义审计；50页跳→三页平均→逐页→跌破往前；未完成不能保存
S5 SAVE_PENDING: 展示临界、前N、排除、max、重复保存检查和点数；用户确认后才保存
S6 SAVE_RUNNING: front保存；等任务finished；用标签结果对账（⚠️companySaveCount=任务进度计数(=total=finished)，非去重敏感指标——防重保存/全新增判断一律以 contactSaveCount/家基线对齐为准,2026-08-31补审P2-1）
S7 TEMPLATE_PENDING: 只生成本地草稿；展示3-8个跨轮模板（★渲染后的收件人视图效果，非HTML源码——用 tools/render_preview.py）+理由；用户确认后才批量创建。★署名规则：落款=昵称；公司名/官网/邮箱仅在用户已主动提供时写入正文，禁止主动索要、禁止编造
S8 TEMPLATE_BUILD: 创建后断言变量样式、标题、正文差异、轮次绑定；失败回S7
S9 SEQUENCE_PENDING: 展示12步(30分/5/15/30天)、时区(★默认纽约)、单日30000/单家5、notSentTags=[询盘,不发]；用户确认后建序列。★客群成交/询盘周期以季~年计时（条件/弱适配）→ 如实告知"节奏为快周期品类设计"，建议调低轮次或转人工培育，禁套固定节奏打扰周期外买家
S9a 固定标签: ★「询盘」「不发」=账号级一次性固定标签（2026-09-03 用户拍板）——先 tags-list 查同名存在即复用其 id，不存在才建；禁每次产品重建；id(名称) 记 runs.tsv 该产品行
S10 CONTACT_PENDING: 保存finished、标签联系人>0、序列inactive、人数对账且用户确认后才contact-add
S11 READY_INACTIVE: 输出完整流程和参数；测试保持inactive，等待用户确认
S12 ACTIVE: 仅明确“确认激活/激活序列<名称>”才激活。激活前 AI 逐项自查并展示：目标市场规则、名单来源、发送主体信息、实际退订入口、拒收机制——发现问题才找用户；用户只对展示结果做最终确认，不需要自行核验
ERROR_BLOCKED: 异常、参数变化、对账不一致或规则冲突时，只读检查，禁止自动写操作
```

### 状态转换与确认
- 高影响状态必须收到**本节点明确确认**后才转换；确认原话、参数JSON/hash、时间、节点写入 `.local/approvals.tsv`（本地流水，不入 Git）。
- “自由发挥/不用看”只豁免**指定节点展示**，不得豁免Gate0、客群、保存、contact-add或激活。
- 产品、种子、临界N、标签、模板、配额任一变化，原确认失效并回到对应状态。
- 可自由执行的只有只读查询、格式整理、读取重试和报告；所有写操作须有本节点确认。
- **★审批硬闸门（工具级）**：写操作工具（`save_first_n.py`/`gen_templates.py`/`rebuild_templates.py`）执行前必须带 `--approval <id> --project <产品>`，工具会校验 `.local/approvals.tsv`（id+project+state前缀+status∈confirmed/backfilled；本地流水，不入 Git）；无凭证或状态不符**直接拒绝写入 exit(1)**。approval_id 由编排器/确认节点输出（见 `tools/approval.py`）。★换机器/新 clone 后 `.local` 为空，写操作前须经确认节点重新生成凭证（`approval.py` 首次建文件自动写表头）。

### 面向用户的输出纪律（B7-4）
- 任何英文错误/术语（FAIL:/ERR:/None/undefined/null/接口 message）出现在给用户看的输出前，**先给一句中文解释 + 下一步**；不确定就说"这是系统内部日志，我来看，你不用操作"。
- 禁止裸 `None` 面向用户（改"未取到"）；错误要说明"能否安全重跑"。

### 节点输出标准
- 客群：每个客群写“精准潜在客户：是/否/条件成立时是”，并给周期、询盘速度、量级、邮箱可得性、竞争度、推荐。
- 模板：展示3-8个跨轮代表；标题不插变量；正文变量保留编辑器完整code样式；客户有实际参数必须照用，无信息才用行业合理值。
- 完成：测试模式输出完整流程、task id、实际数量、模板/序列映射和问题；默认不激活。


## ⚠️ 核心铁律（违反即错）
0. **排除中国/中国香港/中国台湾/中国澳门**（4区 `CN,TW,HK,MO`）——**保存与 S4 扩量搜索（域名搜审计）要排除**；**S3 种子发现文本搜不排除**（★2026-08-31 用户拍板：找种子阶段无需排除，种子客户搜出来、确定之后再排除——排除在 S4/S5 执行；filters 使用完整 exclude schema（见 API 参考））
   - save_first_n.py 默认 `--exclude CN,TW,HK,MO`；domain-scale-sop 已固化

1. **保存用 `selectOption:"front"`**（≠"current"，current邮箱0），`contactMaxCount:3`（★默认3，数据不足才按3→6→9升阶）
2. **模板变量用 `<code class="lfxFieldVeriable" contenteditable="false">{联系人:名称}</code>`**（否则不替换），标题纯文案，差异≥30%
3. **不翻页收集id**（封号）——保存前N用 selectTotal
4. **验证用 `backend-task-status`**（contactSaveCount），**不用** company-save-list（误显邮箱0）
5. **发信前**：去重（★单日上限30000/单家5=用户拍板；notSentTags=[<tagId>(询盘), <tagId>(不发)]；★schedule_id 一律运行时 `tools/resolve_schedule.py` 解析——各账号 id 不同，禁止硬编码）。**责任分界**：来发信系统通道负责 SPF/DKIM/DMARC、发送基础设施和退订技术呈现；运营方不需要在本流程配置这些技术项，但**仍须在激活前核验**目标市场规则、名单来源、发送主体信息、实际退订入口、拒收名单、主体类型和数据处理要求。平台技术能力不等于运营方免除合规责任。
6. **★时序：等联系人保存任务 `status:finished` + 标签联系人>0 后，才能 contact-add/加视图到序列**（提取未完成=标签下没人=add 0）
7. **★标签=`语言-行业-角色`，不是你的产品**（用户纠正 2026-08-30，格式升级 2026-08-31）：购买者不一定是卖同产品的人——标签名写**客群身份**（例 `英语-水上运动-经销商`、`西语-水上运动-经销商`；旧例 `水上运动行业客户`=未分语言的 legacy 格式）；产品归属记在 `runs/<运营方>/<产品>/` 档案。**记录一律 id(名称) 成对**（只记 id 换会话查不到名，tags-list 可查）
9. **★语言三方配对铁律**：标签、模板、序列的**语言必须一致**才许 contact-add——标签前缀语言 ∈ 模板名前缀语言 ∈ 序列市场语言（西语客群禁收英语信；模板分组按 `语言-产品` 建组，序列按语言各建，时区按语言市场 `resolve_schedule --tz` 解析）。**legacy 兜底**：无语言前缀的旧标签（如"水上运动行业客户"）→ 按其现关联序列/模板的实际语言判定；新客群一律新格式，禁止再建无前缀标签
   **★种子语言不限制序列语言（2026-08-31 用户拍板）**：种子=向量锚（任何国家的好锚点都行，ES/FR 公司做种子不影响英语序列）——序列语言由**保存时的国家筛选/标签**决定，不由种子国籍决定。非英语种子的价值=挖到英语竞品挖不到的邻域；保存时可加 country_code include 筛选只存英语区
8. **★内部命名一律中文**（标签/视图/序列/模板分组名——用户都是中国人做外贸）；**邮件正文语言=目标市场语言**（默认全球英语；运营方昵称等档案存本地 `.local/operator-profile.md`，不入 Git）


## 🛡 操作对抗审查（空白子代理，★用户强制 2026-08-31）
> **原则：每个决策/产出必须经空白子代理对抗审查**——空白=无会话上下文的新代理，只喂：仓库路径+规则文件清单+待审产出（计划或结果）+对应审查清单。

**分级（防拖垮节奏）**：
| 操作/产出 | 预审（执行前） | 后审（产出后） |
|-----------|--------------|--------------|
| 保存/建序列/激活/清空 | ✅ 必须过审才执行（与用户确认并行发起，双过才执行） | ✅ 必审 |
| 客群分析/种子筛选/模板文案/序列配置（决策产出） | 可并入确认节点 | ✅ 必审（产出即审） |
| 只读查询/格式整理 | 免 | 免 |

**四类产出的固定审查清单**（审查代理照单执行，不得自由发挥成走过场）：
1. **客群分析（S2 产出）**：①客群真实性=语义判据（是真独立客群还是同一客群换说法）②五维评分每项有据（产能=query_total+AI评估，非拍脑袋）③英文搜索词地道可搜（母语者会这么搜吗）④排序逻辑（客群确认→产能为主→询盘快→周期短）⑤语言/市场标注
2. **种子筛选（S3 产出）**：①是客户方不是用户自己公司 ②**搜索锚=客群 query_en 关键词**（AI 数据库搜索；勿拿公司名当锚 L-45；域名取法=结果 id→domain/base-info）③精准度证据（L-46：P1+P50 角色构成>70%买家才用）④邮箱率数据 ⑤非4区 ⑥为何选此不选彼
3. **邮件模板（S7/S8 产出）**：①语言与目标客群一致 ②买者视角三要素（痛点+利益+行动）③事实可举证（标准号写到底/数字有来源/无假前提假稀缺/无漂绿）④spam 词 ⑤差异 check_template_diff 实测≤0.70 ⑥变量格式+标题纯文案 ⑦**首句=钩子（对方视角）非能力陈述**（sequence-config 钩子规范：抽 3-5 封首句须勾住买家；"We manufacture/Our factory…" 开场=违例）
4. **序列配置（S9 产出）**：①12步步长 30分/5/15/30 ②时区与语言市场匹配（英语→纽约，西语→马德里/圣保罗）③30000/5/notSentTags ④语言三方配对 ⑤tmap 网格校验

**★执行前对抗反思矩阵（2026-08-31 用户拍板：全节点双闸——动手前反思 + 产出后审查，缺一不可）**：

| 节点 | 执行前反思维度（编排方自问并留痕） | 产出后审查（对应清单） |
|------|----------------------------------|----------------------|
| S2→选客群 | 客群真独立吗(非换说法)/规模/邮箱可达/竞争密度/**采购逻辑链成立吗** | 客群分析清单 |
| S3→文本搜种子 | 候选属哪条客户线(直采/OEM/拓品)/邮箱国家证据/为何不是国内同行 | 种子筛选清单 |
| S4/S5→保存确认前 | 临界稳健吗：窗口完整/两口径收敛/敏感性/铁律核对/重叠风险/点数预算 | 确认材料审查（非四类固定清单，S5 展示清单为准） |
| S7→生成模板前 | 卖点真实吗/痛点假设对吗/**有没有不该承诺的** | 模板文案清单 |
| S9→建序列前 | 规则=拍板默认吗/语言三方配对 | 序列配置清单 |
| S10→加人前 | 人数对账/序列 inactive | 时序守卫(工具级) |
| S12→激活前 | 目标序列id逐字核对/预算/期望管理 | 激活后效果复盘（后台报告+询盘登记） |
| **清空/删除前** | **不可逆性确认/范围逐字核对（标签·序列·模板·联系人 id）/备份留档/L-24 教训** | 删除后核对（范围外零伤） |

留痕方式：反思结论一行写入确认材料或 ops-log detail（"对抗反思:过/发现的坑"），未过不得触发动作。

**审查代理权限**：读仓库文件 + **只读线上抽验**（给只读 token，抽 1-2 条核数据真实性——防被本地假记录骗）。
**产出**：本地对抗审查记录（`rev-<日期>-<时分>-<操作>.md`，不入 Git），结论只有 **放行 / 整改清单(P0/P1/P2)**；P0=操作不得执行或须回滚；整改后重审。
**凭证链**：review 文件与 approvals.tsv 并列；写操作三凭证齐备才合规：用户确认(approvals) + 对抗审查(reviews) + 操作流水(ops-log)。

## 📊 记录体系四件套+三层索引（★2026-08-31 立）
```
runs/<运营方>/<产品>/
├── seeds.tsv          ★L0 公司明细（每联系人一行：id/公司名/域名/国家/邮箱——询盘反查基础；保存完成后按标签全量拉取落盘）
├── segments.md        L2 产品索引（生成物——由 segments/ 各档重生成，手改无效）
├── segments/          L1 客群详情（★唯一真源，每客群一档，字段字典见 segments/README.md）
├── ops-log.tsv        操作流水（append-only 13列，本地写入器强制校验，拒绝手写缺列）
├── templates.md       模板登记+效果（模板id=主键；轮次级效果由后台报告会话首拉）
└── inquiries.tsv      询盘记录（segment_id+template_id 两列=客群×模板效果分析基础）
本地客群总表           L3 全局总表（生成物，跨产品；本地数据，不入 Git）
runs/INDEX.md          运营方×产品导航卡（生成物，源自 runs.tsv+segments.tsv 汇编）
```
- **生成链**：L1 详情 → AI 更新后重生成 L2/L3/INDEX（手改生成物无效）；本地运行记录（db/runs.tsv，本地数据，不入 Git）仍是产品级操作真源（不加列，语言从标签前缀读）
- **L2/L3/INDEX 的 saved/emails 生成源**：取该客群 seeds 表**最新一次成功行**（结果列含✅）；legacy 合并值统一写 `(合并:N)` 格式（不可聚合，仅供溯源）；INDEX 唯一数据源 = 本地运行记录 + 本地客群总表 汇编（本地数据，不入 Git）
- **ops-log 保存行必填（6项）**：segment_id / saved_n / emails_n / company_tag / contact_tag / task_id——哪天存了哪个客群多少到哪个标签，一查即得；**sequence_id 不在保存行必填**（S5 保存先于 S9 建序列，时序不可满足）——序列归属由后续 `--op sequence` 行按同一 segment_id 登记；append-only 无技术拦截，以 git 历史为改写凭证
- **客群 ID 规则**：产品内 S01-S99 唯一永不复用；语言进名称不进 ID

## 📌 对抗固化（Terra验证后）
- **去重机制（实证）**：重复保存同一批 → 系统去重 → **contactSaveCount 只算新增**（实测 19689→46→0，batch均为9990家）
- **contactSaveCount = 本次新增(去重后)写入数**（≠该批总数）
- **防重复保存（省额度）**：保存前查该 `(keyword+seed+阈值+selectTotal+排除4区)` 是否曾完成保存（用完成标志/最近成功task），已保存则**不重存**（否则幂等重存+点数守卫）
- **测试不激活（★用户强制）**：流程跑完 → **发完整流程待确认**，**不激活序列**；**仅用户明确正向命令（"确认激活"）才激活**，禁止自行激活

## 🔄 任务类型 → 状态/进度接口对照（★必背，别用错）
| 任务类型 | 查询接口 | id 字段 | 返回 |
|----------|---------|---------|------|
| **保存**（cluesSave，save_first_n返回 data.id）| `operation/backend-task-status` `{"type":"cluesSave","id":<task id>}` | data.id（save返回）| status/finished/total/**contactSaveCount** |
| **删除**（contactsDelete，delete返回 backendId）| `operation/backend-progress` `{"id":<backendId>}` | data.backendId（delete返回）| status/total/finished/**progress** |
| **template/序列** | 直接查对应 list 接口 | - | - |
> ⚠️ backend-task-status 查删除=只返回id无进度（L-36 误判教训）；保存任务用 task-status（有contactSaveCount），删除用 backend-progress。
> ⚠️ **token 来源**：每次由用户提供（web.laifaxin.com&<orgId>&<hash>），写在命令/环境；gate_check.sh --token 用同一 token。

## ★ 模板变量（★字段清单=记录；实际使用=策略，勿滥用）
- **格式**：`<code class="lfxFieldVeriable" contenteditable="false">{联系人:名称}</code>`（{联系人:<title>}/{公司:<title>}，title=字段中文标题）
- **字段来源**：`fields/contacts-fields`(contact) + `fields/company-fields`(company) → data[].title（**全量清单记录在 api-reference，勿乱编**）
- **★使用原则（用户拍板）**：
  - **✅必用**：`{联系人:名称}`（唯一非空可靠+自然）
  - **⚠️慎用**：`{联系人:职位}`（仅确认有值才插，缺失则句子断裂）
  - **❌禁用(正文)**：`{联系人:邮箱}`（放收件人自己邮箱=荒谬+自证批量）
  - **❌默认禁用**：`{联系人:公司名称}`(c_name)/`{公司:公司名称}`(orgName)/`{公司:域名}`等（错位+暴露数据源+空值；仅用户明确要求才用）
  - **记录≠使用**：字段清单全记录(api-reference)，模板只用必要最小集；**空字段表现未验证前，只保证名称非空**
## 🎯 关键节点确认机制（★用户要求，默认确认；除非用户明确"别给我看/自由发挥"）
**分层**：
- **决策节点（必须用户确认，给建议+理由，禁止直接执行）**：
  | 节点 | 展示给用户 | 建议维度 |
  |------|-----------|---------|
  | ①推演客群 | 打印全部客群(4/8个) | 成交周期/询盘快慢/市场量级/邮箱可得/竞争度——**给出推荐** |
  | ②选种子 | 候选种子+代表客户 | 精准度/邮箱率/是否会采购 |
  | ③临界值 | 临界页/保存N | 数据质量/是否达标 |
  | ④模板 | **展示3-8个代表模板**（不同轮次/角度；v2 新档 5-8 封） | 为何这样写/对应客户场景——确认后生成120 |
  | ⑤激活 | 流程待确认 | 不激活，仅明确指令才激活 |
- **技术执行层**：只读查询、审计取样、格式整理可自由执行；建标签、保存、批量模板、序列、contact-add均须对应节点确认
- **默认**：决策节点必须确认。用户说"不用看/自由发挥"时，只能跳过**展示环节**并采用推荐方案；建标签、保存、批量模板、建序列、contact-add、激活和清空等写操作仍须对应节点明确确认与审批凭证，任何口头豁免都不能跳过。
- **建议原则**：每个决策给出**推荐+理由**（成交周期短/询盘快/量级大 = 推荐），用户可改

## 📂 子规则（引用，非重复）
- 临界：`specs/threshold-method.md`（AI反思/三页平均/50页跳）
- 适配：`specs/product-fit.md`（★S0 前置：产品适配度强/条件/弱三档判定+各档打法+弱适配话术）
- 规模化：`specs/domain-scale-sop.md`（域名搜/front保存）
- 模板：`specs/sequence-config.md`（code变量/差异30%）
- 序列：`specs/sequence-config.md`（12步步长30分/5/15/30天/美国时间/发送上限/禁发询盘）
- 平台侧缺口（内部整改清单，未随库分发）：认证/合规/冷启动/去重 P0-P3——以当前规则与 market-rules-2.0 表述为准

## 🔄 迁移/持久性
- **规则都在本仓库**（本地知识库，git/外置盘可同步）
- **换电脑**：克隆/拷贝本仓库 → 读 `规则总纲` → 按 specs 执行
- **新会话**：先读 `规则总纲` + 关键 specs，再操作（不凭记忆）
- **工具**：`tools/` 下脚本（save_first_n.py、audit_company.py、gen_templates.py）都封装了正确规则，**优先调用脚本**（工具=规则；create_*_12rounds 旧模板脚本已 deprecated，勿用）

### 多公司/多产品（★2026-08-30 对抗重构，其他人拿到直接用）
- **通用层**（RULES/specs/lessons/tools/INDEX）不分公司共享；**档案层** `runs/<运营方>/<产品>/` 每产品一份完整记录（operation-record.md + reflection.md + evidence.json + verify-*.txt + tmap.json）
- **运营方档案**（本地 `.local/operator-profile.md`，不入 Git）：昵称为必需；公司名/官网/联系方式=用户主动提供才记录（★禁止主动索要；没提供时落款只用昵称、正文不写这些字段）
- **新公司/新产品接手**：复制 `runs/_template/` → 填运营方档案（本地）→ 按状态机跑；不同公司档案互不干扰
- **回落**：产品跑完 → 写 `runs/<运营方>/<产品>/` 三个文件 + 更新本地运行记录一行（含 nickname/created/updated；本地数据，不入 Git）
