# S7 模板草稿展示
```
【模板预览 · <客群名>（第 <i>/<K> 批）】开发信分 12 轮跟进（首封 30 分钟后、之后 5/15/30 天一轮），每轮 4 个变体共 48 封。

本批只针对「<客群名>」这一个客群写；其他客群会各写各的一套（不同痛点、不同话术）。

落款：<昵称>（您的纯个人昵称）——邮件末尾签名区只有这一行，不含公司名、网址、邮箱、职位、认证或宣传语。
产品事实：本批正文只用了您确认过、且有可靠来源的信息：<列出用到的卖点/数字>。没有可靠来源的认证、交期、价格、具体数字，我一律没写进去。

下面是 <3-8 封，新档建议 5-8> 封不同轮次的收件人视图样例：
---
<粘贴渲染后正文，含 Hi 【名称】>
---
生成后会自动归入分组「<模板前缀>」（分组名=语言-产品，中文命名）——平台模板库左侧目录树点开即看全部 48 封。
看原文/全部：打开 https://web.laifaxin.com/settings/templets 搜「<模板前缀>」

回复"确认生成"批量创建，或说要改哪里（语气/卖点/称呼）。
```
## AI 执行要点与边界
- 邮件末尾签名区只用用户纯昵称；公司名/官网/邮箱/职位/认证/宣传语等任何其他内容不得进入签名
- 正文卖点可使用 confirmed product-profile 中有字段级来源的认证/产能/MOQ/交期/价格带；生成计划绑定 profile hash，高风险事实逐句 claims 校验；declined 档案只用通用无具体事实口径
- 公司名/官网/联系邮箱存 operator-profile 供 AI 理解企业，不自动写进正文；客户/联系人第三方资料不进入两类档案
- 展示 3-8 封**渲染后收件人视图**（非 HTML 源码；新档建议5-8）；跨轮各选一封
- 生成后必跑 check_template_diff（Jaccard≤0.70），失败回本节点
- **可选的网页版预览**（给想看整体效果的 AI/用户）：`python3 tools/render_html_preview.py --plan <template-plan.json> --rounds 1-4 --variants 1,2` → 生成 `preview.html`（本地文件；加粗卖点/回复关键词黄色高亮、CTA 段蓝线标注，浏览器直接打开）。**命令只写给 AI，不要贴进用户话术块**
- **★每批一套模板（铁律 7d）**：N 个客群 = N 套模板（默认每套48），各套痛点角度不同；标题必须写「<客群名>（第 i/K 批）」，禁止一套模板打多个客群
- **★前缀带客群标识**：模板名前缀含客群标识（如 `英-皮筏艇-经销商-`），与标签名呼应，便于平台按分组核对
- **用户可见文案纪律**：话术块内不出现 `sha256`/`claims`/`product-profile.md`/工具命令等内部术语
