# S12 激活确认（AI 先自查，用户只确认）
```
【激活前自查结果】

💡 **一句话结论**：6 项发送前检查**全部通过**，可以放心激活。下面是逐项明细，不感兴趣可以跳过。

| 核对项 | 结果 |
|---|---|
| 目标计划 | <序列名>（逐字核对过 ✅）|
| 12 步与模板 | 已逐项验证通过 ✅ |
| 不发送保护 | 询盘/不发标签已挂 ✅ |
| 发送上限 | 每天最多 30000 封（全账号）；同一家公司每天最多 5 封 ✅ |
| 发送合规五项 | 目标市场 / 名单来源 / 发送主体 / 退订入口 / 拒收机制——都已逐项核验通过 ✅ |
| 产品档案 | 已由您确认、内容与来源都锁定 ✅ |

⚠️ 激活后邮件将按计划真实发出。

👉 **现在轮到您，两种方式任选一种**（效果一样，哪种顺心选哪种）：

**方式一 · 让我来（推荐，省事）**
回复一句：**确认激活 <序列名>**　→ 我立刻执行并核对结果。

**方式二 · 您自己去网页上激活（想亲眼看一遍数据就选这个）**
1️⃣ 打开 https://web.laifaxin.com/marketing/sequences
2️⃣ 找到这条计划 **<序列名>**，点开核对：12 步、里面的模板、每天发送上限、不发送保护
3️⃣ 确认无误后，把这条计划的开关打开（开启）
4️⃣ 回来跟我说一声：**我已在网页激活 <序列名>** → 我核对平台状态后接着往下走

（两种方式都由您决定、都记在账上；区别只是"我替您点"还是"您自己点，我核对"。）

您确认之后：计划生效，平台会按后面给您看过的时间表自动跟进发出。
**有人回复不会自动停**——收到回复后由我（或您）立即打上“询盘”标签，标签生效后才停后续邮件；
明确退订/拒绝的打“不发”标签。**您随时说停，我立刻停**。
发送只在纽约工作时间（周一至周五）进行；若现在不在该时段，第一封会在下一个工作时段发出。
```

## AI 执行要点与边界
- 仅用户明确"确认激活/激活序列<名称>"才执行；犹豫/否定句一律拒绝
- 激活前 AI 逐项自查并展示：序列id逐字核对/12步验证/不发送保护/发送上限/市场规则/名单来源/发送主体/实际退订入口/拒收机制
- 合规文件顶层必须 `evidence_mode:"live"`；五项 source/detail/checked_at 必须是72小时内真实核验，任何 simulation/mock/stub/离线/网络桩/占位/未实际标记都拒绝签凭证和激活
- 已到 S11/READY_INACTIVE 的项目，用 `flow_orchestrator.py --resume-s12 --org <ORG_IN_MEMORY> --profile <标准档案> --seq <序列id> --compliance-file <合规文件> --confirm "<用户原话>"` 签发 S12 凭证（**用户在自己聊天框里回复即可，不需要开终端**；原话须含"激活"字样）。该模式不联网、不激活、不重跑 S0-S10，签发后 record 仍保持 S11。
- ★**为什么要用户亲口说**：激活后邮件就真的开始发。所以必须是用户自己说出"确认激活"，AI 不得用含糊应答（"好的""可以"）代替——工具会拒绝不含"激活"的原话。
- **S12凭证唯一出口**：只有项目已真实收口 S11 后，才能使用上行 `--resume-s12`；普通 flow 走到末尾永远只记 pending并提示先完成S11，不得签S12。`approval.py grant` 同样禁止S12
- 凭证签发后，再由主 AI 在内存中调用 `activate_sequence.py --profile ... --compliance-file ... --record ... --confirm '<同一原话>' --approval <凭证>`；激活工具必须真实回读 active
- **★方式二（用户自己在网页激活，2026-09-17 用户要求）**：用户在 https://web.laifaxin.com/marketing/sequences 手动开启后，
  主 AI 用 `activate_sequence.py --sync-manual --org <orgId> --seq <id> --record <record> --confirm "<用户原话>"`
  同步本地状态——该模式**只读回读平台确认 active + 写本地 record**，**不发起任何平台写操作、不需要审批凭证**
  （用户在网页上亲手开了开关=已行使同一意志；审批凭证用于授权"AI 代为写平台"，此路径无平台写操作）。
  回读非 active 一律拒绝并保持 S11（常见原因：开关没生效/开在别的工作空间）——不得猜、不得替用户激活。
  两条路径都要求原话含"激活"；方式二同样要逐字核对 seq 与 record 一致（防串台）。
- 用户只对展示结果做最终确认，不需要自行核验技术项

## 激活后建议（激活成功后的第一条汇报消息里带上）
- **当天就能看到数据**：首封发出后数小时内陆续送达/打开，不必等三天——登录 web.laifaxin.com → 「邮件追踪」（或「智能跟进计划」里点该计划），当日即可看送达/阅读/点击次数
- **首日样本参考**：名单 ≥5000 当天即可看出送达/打开趋势；名单只有几百的话，当天只看退信、别看趋势
- 退信率 >10% 随时喊停（可一键回滚 inactive）；正常情况下建议**当天傍晚看一次**送达/打开，第 2-3 天看回复
- **48 小时裁决**：数有效询盘（同行套价/自动回复不算）——**≥3 个=合格可扩量**（询盘方网址当新种子回流）；**<3 个=偏低**，按 docs/09 诊断漏斗（送达→打开→回复）后再决定下一波，连续两轮不达标→建议止损
