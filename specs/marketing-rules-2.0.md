---
title: "营销获客规则 2.0（用户拍板 + 对抗升级）"
description: "规则2.0：默认70%/每司3邮箱(覆盖更多公司)/暖机针对系统通道不同；用户拍板版"
created: 2026-08-25
updated: 2026-08-25
author: "用户拍板 + 对抗升级"
source: "用户三点澄清 + 预算对抗分析"
related: [specs/operations-sop, specs/sequence-config]
tags: [规则2.0, 70%默认, 3邮箱, 暖机, 覆盖优先]
status: verified
audience: 人+AI
---

# 🏆 营销获客规则 2.0（用户拍板 · 对抗升级）

> 覆盖1.0。**用户拍板**：
>  1️⃣ 默认 **70%**（用户特殊要求才 80/60）
>  2️⃣ 暖机需讲清"什么规则"（系统通道场景）
>  3️⃣ 每公司**默认 3 邮箱**，预算省下**存更多企业**

## 【1. 保存标准：默认 70% ✅ 用户确认】
| 档 | 阈值 | 何时用 |
|----|------|--------|
| 中等（默认）| ≥70% | **默认执行线** |
| 严格 | ≥80% | 用户特殊要求（高价值客群）|
| 宽松 | ≥60% | 用户特殊要求（扩量/冷启动）|
- ✅ 默认 70%，**除非用户要求 80/60**

### ★ 判断标准 = AI 语义反思（不是关键词匹配）
- 每条客户，**AI反思"这个外贸 B 端客户会不会买我的产品？"** 能买=匹配
- 靠**读描述推理业务链**（促销/礼品/饮具/户外/装备/渠道商等可能采购），**绝不靠"含某词"**
- 工具词匹配只做趋势初筛，**结论必须 AI/人工语义判断**（L-26）
- 临界点判定详见 [threshold-method.md](threshold-method.md)

## 【2. 暖机规则（系统通道场景 · 对抗修正）】
> **关键澄清**：序列用 `channel:system`（系统通道），域名/IP 是来发信的，**用户不需要自己暖机域名**。红队"暖机"主要针对自有邮箱/自建域名 —— **与我们场景不同**。

### ✅ 本场景的暖机 = 控制"发信节奏 + 列表规模"，不是控域名
| 项 | 规则 |
|----|------|
| 发送上限（max_emails_per_day）| ~~从低起步（如 500，非 30000）~~ → **★已被最终拍板覆盖=30000**（发送上限拍板记录 closed 2026-08-29，用户拍板，"冷启动800"已废；保留的暖机建议=实际**发送节奏**，见下行）|
| `domain_emails_per_day`（单域名）| 5（防同域名过度触达）|
| 新列表/新客群 | 先小批验证（几千封）→ 看送达/回复 → 再放量 |
| 健康线 | 退信 < 2-3%，垃圾投诉 < 0.1-0.3% |

### ⚠️ 对抗修正（★配置部分已被拍板覆盖）：30000 最终=用户拍板配置值
- ~~`max_emails_per_day:30000` 是**全平台所有域名/账号之和**，**单序列起步别设 30000**~~ → **已推翻**：发送上限拍板记录 用户最终拍板 `max_emails_per_day=30000`（sequence-config 已固化；RULES S9 展示口径=单日30000/单家5）
- 仍有效=**发送节奏**建议：按列表规模从低量起、随送达/回复提升（暖机=节奏非配置）

## 【3. 每公司 3 邮箱（用户拍板 · 预算覆盖更多企业）✅】
### 预算对抗分析（10万点·60%预留=6万）
| 方案 | 每司成本 | 公司数 | 总邮箱 |
|------|---------|--------|--------|
| **A. 3邮箱（默认）** | 1.5×3=4.5点 | **13333家** | 40000 |
| B. 5邮箱（红队） | 1.5×5=7.5点 | 8000家 | 40000 |

**结论**：3邮箱 vs 5邮箱 **总邮箱数几乎一样**（≈40000），但 **3邮箱覆盖多 5333 家公司**（触达更广）。
→ **用户直觉对**：默认3邮箱，省预算**存更多企业**（广覆盖）。

### ✅ 规则：每公司默认 3 邮箱（上限，可按用户要求上调）
- 默认 `contactMaxCount: 3`
- 用户可上调（存更少公司但每司多触角）
- 预算公式：`可存公司数 = (点数×60%) ÷ (1.5×3)`

## 【4. contactExcludes 逻辑（对抗修正）】

> ⚠️ **用户拍板（2026-08-30）：no-reply 也不排除——contactExcludes 一律留空 `[]`（save_first_n 现状即正确）。本节"只排 no-reply"建议作废，保留作讨论记录。**
> 红队建议传，但**要分两类**，避免误杀：

| 类别 | 邮箱 | 处理 |
|------|------|------|
| ❌ 绝对排除 | noreply@/no-reply@/do-not-reply@/mailer@/postmaster@ | 排（非真人）|
| 🟡 通用邮箱桶 | info@/sales@/contact@/support@/admin@ | 保留，但**打"通用邮箱"标签**（低优先）|
| ✅ 优先 | 姓名邮箱（xxx@）| 进主序列 |
| ⚠️ 未知 | unknown(catch-all) | 单独验证，不混批 |

**对抗**：不传 contactExcludes 会命中 no-reply 导致退信；但全排除 info@ 会漏小公司主邮箱 → **折中：只排 no-reply 等确定非真人，info@ 保留但降优先**。

## 【一句话总原则 2.0】
**默认70%保存；每公司3邮箱（存更多企业，预算广覆盖）；系统通道不用暖域名，控发信节奏+列表规模；contactExcludes一律留空[]（★2026-08-30拍板:no-reply也不排除）；统计按createAt分批。**

## 🔗 相关
- 规则 1.0（已过时，未随库分发）；本文件为现行 2.0
- 执行见 [operations-sop.md](operations-sop.md) / [domain-scale-sop.md](domain-scale-sop.md)
